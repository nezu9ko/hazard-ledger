# 隐患治理台账系统 — 维护手册（详细版）

> 面向对象：负责日常维护本系统的非专业程序员
> 最后更新：2026-09-22

---

## 目录

1. [先读这一章：三个必须记住的规则](#一先读这一章三个必须记住的规则)
2. [文件地图：哪个文件管什么](#二文件地图哪个文件管什么)
3. [日常运维：启动、停止、重启、看日志](#三日常运维启动停止重启看日志)
4. [常见修改：改哪里（速查表）](#四常见修改改哪里速查表)
5. [修改示例：手把手教你改 4 种典型需求](#五修改示例手把手教你改-4-种典型需求)
6. [数据库：查数据、改数据、加字段](#六数据库查数据改数据加字段)
7. [Git 版本管理：改坏的后悔药](#七git-版本管理改坏的后悔药)
8. [故障排查：症状 → 原因 → 处理](#八故障排查症状--原因--处理)
9. [备份与恢复](#九备份与恢复)
10. [安全注意事项](#十安全注意事项)
11. [附录：命令速查表](#十一附录命令速查表)

---

## 一、先读这一章：三个必须记住的规则

### 规则 1：改前端 = 刷新浏览器；改后端 = 重启服务

| 你改的文件 | 生效方式 |
|-----------|---------|
| `public\index.html`、`public\style.css`、`public\script.js` | **只要刷新浏览器**（`Ctrl` + `F5` 强制刷新清缓存） |
| `server.js`（后端逻辑） | 双击 **`restart-service.bat`** → 选「以管理员身份运行」 |
| `config.json`（端口、数据库连接） | 同上 |

> 这是最容易搞错的地方。改完前端发现没变化，多半是浏览器缓存，`Ctrl+F5` 就好。

### 规则 2：永远用 `restart-service.bat` 重启，不要用 `schtasks /End`

`schtasks /End` 只会结束计划任务的"外壳"，**杀不掉真正的 node 进程**，结果是：
- 旧进程还占着 3000 端口
- 新代码起不来
- 你看到的现象是"改了代码不生效"（很容易误判成代码写错了）

`restart-service.bat` 做的是正确的三步：结束任务 → **按端口找到 PID 并强杀进程树** → 重新启动。

### 规则 3：改之前先备份，最好用 Git 留个"后悔点"

- 最省事的备份：把要改的那个文件复制一份，改名加日期后缀，例如 `script.js.bak-20260921`
- 更规范的备份：用 Git（见第七章），改前提交一次，改坏了 `git checkout .` 一键还原

---

## 二、文件地图：哪个文件管什么

### 2.1 ⭐ 公司标识在哪里（重要）

**代码仓库里不含任何公司名称**，公司名放在 `config.json`（该文件被 `.gitignore` 排除，不会进 Git）：

```json
{
  "companyName": "在此填写公司名称",   ← 改这里
  ...
}
```

**工作原理**：
```
config.json 的 companyName  →  服务端公开接口 /api/app-info  →  前端启动时读取并渲染到侧边栏与登录页
```

- 改公司名：改 `config.json` → **重启服务** → 刷新页面即可
- 留空或配置缺失：界面照常工作，只是不显示公司名（不会报错）
- **换机器部署**：复制 `config.example.json` 为 `config.json`，填上公司名与数据库密码

> 💡 这样设计是为了让**代码可以安全地放到任何地方**（GitHub、U 盘、发给外部），
> 而**公司信息只留在本机**。


```
D:\hazard-ledger-lan\
│
├── server.js  ★核心后端★  所有接口、登录鉴权、数据读写、Excel 导出、图片上传
│
├── public\    ★前端★
│   ├── index.html     页面骨架（只有十几行，一般不用改）
│   ├── style.css      全部样式：颜色、字体、间距、响应式
│   ├── script.js      全部前端逻辑：页面渲染、交互、图表
│   └── logo.jpg       公司 Logo
│
├── config.json        ★配置★ 端口 + 数据库连接（含数据库密码）
│
├── pgdata\            ★数据★ PostgreSQL 数据目录（数据库本体）—— 绝对不要手动改
├── uploads\           ★数据★ 隐患/整改**附件**（图片 + PDF/Word/Excel）—— 绝对不要手动删
├── backup\            数据库自动备份文件（保留最近 14 份）
├── logs\              服务日志归档（server-out.log 超 5MB 自动轮转，保留最近 5 个）
│
├── start.bat          手动前台启动（关掉窗口即停止）
├── run-server.bat     后台启动（计划任务调用的就是它）
├── install-autostart.bat  部署脚本：放行防火墙 + 注册开机自启
├── restart-service.bat ★改完后端代码就用它★
├── backup-db.bat      手动备份数据库
├── 检查环境.bat ★遇到问题先跑这个★
├── 推送到GitHub.bat   一键把代码同步到 GitHub
├── tools\
│   └── check-env.js   环境体检的具体实现（由 检查环境.bat 调用）
│
├── README.md              使用说明
├── 环境复现要求报告.md      换机器怎么重新部署
└── 维护手册.md            本文件
```

### 一句话记住

- **改样子**（颜色、文字、排版）→ `public\style.css` 和 `public\script.js`
- **改数据**（查隐患、改状态）→ 用系统界面，或用 DBeaver 直接查数据库
- **改逻辑**（新增字段、改流程）→ `server.js` + `public\script.js` 两边都要改

---

## 三、日常运维：启动、停止、重启、看日志

### 3.1 查看服务是否在运行

```bat
netstat -ano | findstr :3000
```
看到 `TCP 0.0.0.0:3000 ... LISTENING` 就说明在运行。
（如果没有任何输出，说明服务停了。）

### 3.2 常用操作一览

| 想做什么 | 怎么做 |
|---------|--------|
| **重启服务**（改后端后必做） | 双击 `restart-service.bat`，选「以管理员身份运行」 |
| 停止服务 | 以管理员打开 CMD，执行 `schtasks /End /TN HazardLedger`（注意：可能残留进程，建议改用重启） |
| 立即启动服务（不重启） | 以管理员执行 `schtasks /Run /TN HazardLedger` |
| 手动临时启动（调试用） | 双击 `start.bat`，窗口别关；关掉窗口就停了 |
| 看是否开机自启 | 看 `deploy-setup.log` 是否有 `TaskName: \HazardLedger` 和 `Status: Running` |

### 3.3 看日志（出问题第一步）

| 日志文件 | 内容 |
|---------|------|
| **`server-out.log`** | ★最常用★ 每个 API 请求的时间、路径、状态码、耗时；启动信息；错误堆栈 |
| `deploy-setup.log` | 防火墙与计划任务的注册记录 |
| `backup.log` | 每天的备份执行结果 |
| `restart.log` | 每次重启的记录 |

用法：用 VS Code 或记事本打开 `server-out.log`，**拉到最下面**看最近的记录。
出现 `[ERROR]` 就是出错了，后面的堆栈能看出是哪个功能的问题。

### 3.4 服务没起来的排查顺序

1. 看 `server-out.log` 最后几行有没有报错
2. 检查 PostgreSQL 服务是否在跑：`netstat -ano | findstr :5432`
   - 没在跑 → 运行 `D:\PostgreSQL\start-pg.bat`（管理员）

---

## 四、常见修改：改哪里（速查表）

> 这张表是本手册最实用的部分。想改什么，先在这里找。

| 想改的内容 | 文件 | 具体位置 / 关键词 |
|-----------|------|------------------|
| **公司名称**（侧边栏 / 登录页显示） | **`config.json`** | 改 `companyName` → 见 2.1 节（该文件**不进版本库**） |
| **公司 Logo** | `public\logo.jpg` | 直接替换这张图片即可（建议等比、正方形） |
| 顶栏标题 | `public\script.js` | 搜 `隐患治理台账系统` |
| 登录页标题 | `public\script.js` | 搜 `login-title` |
| **主题蓝色** | `public\style.css` | `:root` 里的 `--primary` |
| 警示橙色 / 成功绿 / 危险红 | `public\style.css` | `--accent` / `--success` / `--danger` |
| 隐患等级名称（重大/较大…） | 两处：`server.js` 的 `LEVELS`、`public\script.js` 的 `LEVEL_LABELS` | |
| 隐患类别名称 | 两处：`server.js` 的 `CATEGORIES`、`public\script.js` 的 `CATEGORY_LABELS` | |
| **新增一个隐患类别** | 上一条同样两处，各加一项 | 注意两边的 key 必须完全一致 |
| 角色名称 | `public\script.js` | `ROLE_LABELS` |
| 新建用户的初始密码 | `server.js` | `DEFAULT_INITIAL_PASSWORD` |
| **登录失败几次锁定 / 锁定多久** | `server.js` | `MAX_LOGIN_FAILS`（默认 5）、`LOGIN_LOCK_MINUTES`（默认 15） |
| 登录有效期（默认 7 天） | `server.js` | `SESSION_TTL_MS` |
| 口令散列强度 | `server.js` | `SCRYPT`（N/r/p，调大更安全但登录更慢） |
| 台账每页显示条数 | `public\script.js` | `listState` 里的 `pageSize` |
| 单个字段最多几个附件 | 两处：`server.js` 的 `normalizePhotos(max)`、`public\script.js` 的 `MAX_PHOTOS` | |
| **支持的附件类型 / 大小上限** | `server.js` | `UPLOAD_TYPES`（图片 8MB，PDF/Word/Excel 20MB） |
| 照片压缩尺寸/质量 | `public\script.js` | `compressImage(file, 1600, 0.82)` |
| **日志轮转阈值** | `run-server.bat` | `MAXBYTES=5242880`（5MB，保留最近 5 个归档） |
| **批量删除上限** | `server.js` | 搜 `单次最多删除 200 条` |
| 备份保留份数（默认 14） | `backup-db.bat` | 搜 `skip=14` |
| 每日备份时间（默认 02:00） | 计划任务 | 见下方说明 |
| **服务端口**（默认 3000） | `config.json` | `port` |
| 数据库连接信息 | `config.json` | `db` 段 |
| 打印稿标题 | `public\script.js` | 搜 `矿山安全隐患排查治理台账` |
| 完成率颜色档位 | `public\script.js` | 函数 `rateLevel`（85/60/30 三个阈值） |

### 改每日备份时间

以管理员打开 CMD：
```bat
schtasks /Change /TN HazardLedgerBackup /ST 03:30
```
（改成每天 03:30 执行）

---

## 五、修改示例：手把手教你改 4 种典型需求

### 示例 1：改主题颜色（最简单，只改一个文件）

假设想把主题蓝改成深绿色。

1. 用 VS Code 打开 `public\style.css`
2. 在文件最上面找到：
   ```css
   :root {
     --primary: #1e40af;
   ```
3. 把 `#1e40af` 改成 `#15803d`，保存
4. **刷新浏览器**（`Ctrl+F5`）

> 所有用了 `var(--primary)` 的地方会一起变色（侧边栏、按钮、表头等）。想改得更彻底，把所有 `--primary` 相关的值都换掉即可。

### 示例 2：改一个文字（只改前端）

假设想把「隐患登记」改成「隐患上报」。

1. 打开 `public\script.js`
2. `Ctrl+F` 搜 `隐患登记`
3. 会搜到菜单项和页面标题两处，都改掉
4. 保存 → 刷新浏览器

### 示例 3：新增一个隐患类别（前 + 后都要改）

假设要加一个类别「通风安全」，机器码定为 `ventilation`。

**第一步：后端 `server.js`**
```js
const CATEGORIES = ["equipment", "operation", "fire", "electrical", "environment", "management", "ventilation"];
```
（在数组末尾加上机器码）

**第二步：前端 `public\script.js`**
```js
const CATEGORY_LABELS = {
  // ...原有项...
  ventilation: "通风安全",     // ← 新增这一行
};
```

**第三步：重启服务**
双击 `restart-service.bat`

> ⚠️ 关键：两边的 key（`ventilation`）必须**完全一致**，否则前台显示会出问题。
> 勾选「文件 → 保存」，然后重启后端、刷新前端。

### 示例 4：新增一个字段（比如「隐患发现方式」）

这是最复杂的一类，需要动 4 个地方。以新增 `discoverMethod`（发现方式：日常检查 / 专项检查 / 群众举报）为例：

**① 数据库加列**（用 DBeaver 或 psql 执行）：
```sql
ALTER TABLE hazard ADD COLUMN IF NOT EXISTS discover_method TEXT;
```

**② `server.js` 三处**
- `rowToHazard()` 里加映射：`discoverMethod: r.discover_method ?? null,`
- 登记接口（`handleHazards` 的 POST 分支）的 INSERT 语句里加上该列
- 若允许修改，`PATCH` 分支的字段白名单里加上

**③ `public\script.js` 两处**
- 登记表单：仿照现有字段加一个 `<select>`
- 详情页：加一行展示

**④ 若打印/导出也要带**：`printHazardDetail()` 和 `handleExport()` 里各加一列

**⑤ 重启服务 + 刷新页面**

> 建议：这类改动交给熟悉代码的人做，或让我协助。改完务必用系统的「登记 → 详情 → 导出」全流程验证一遍。

---

## 六、数据库：查数据、改数据、加字段

### 6.1 用什么工具

**DBeaver Community**（免费）—— 图形化操作，能看到所有表和数据，不用记命令。
连接信息：

| 项 | 值 |
|----|----|
| 主机 | `127.0.0.1` |
| 端口 | `5432` |
| 数据库 | `hazard_ledger` |
| 用户名 | `postgres` |
| 密码 | 见 `config.json` 的 `db.password`（**口令不写入任何代码或脚本**） |

### 6.2 数据表说明

| 表名 | 存什么 |
|------|--------|
| `hazard` | **隐患台账**（主表，一条隐患一行） |
| `users` | 用户与角色 |
| `sessions` | 登录会话令牌（可随时清空，只是会让所有人重新登录） |
| `operation_log` | 操作日志（谁在何时做了什么） |
| `counters` | 隐患编号计数器（按日期） |

### 6.3 字段中文含义（已写进数据库）

**所有表和字段都已加了中文注释**，在 DBeaver 里能直接看到，不必再查文档：

| 工具 | 在哪看注释 |
|------|-----------|
| **DBeaver** | 双击表 → 下方「**列**」标签页 → 看「注释」列；或鼠标悬停列名 |
| **DBeaver（数据网格）** | 表头右键 →「显示列描述」 |
| psql | 执行 `\d+ hazard` |

> **注释的"源头"在 `server.js` 的 `SCHEMA_COMMENTS` 数组**（51 条：5 张表 + 46 个字段）。
> 服务每次启动都会自动同步一遍，所以**改了注释文案只需重启服务**即可生效。
> 另有一份生成好的 SQL 参考：`tools/db-comments.sql`（换机器时可手动执行）。

**常用对照**（应急时手边没有工具也能查）：

| 表 | 字段 | 含义 |
|----|------|------|
| hazard | `hazard_code` | 隐患编号 YH-YYYYMMDD-NNNN |
| hazard | `location` / `description` | 隐患部位 / 隐患描述 |
| hazard | `level` | 等级：`major`重大 `serious`较大 `general`一般 `minor`轻微 |
| hazard | `category` | 类别：`equipment`设备设施 `operation`作业行为 `fire`消防安全 `electrical`电气安全 `environment`环境安全 `management`安全管理 |
| hazard | `status` | 状态：`pending`待整改 `rectifying`整改中 `closed`已闭环 |
| hazard | `plan_deadline` | 计划完成日期（过期未闭环即「逾期」） |
| hazard | `closed_at` | 闭环时间（看板"闭环耗时"= closed_at − created_at） |
| users | `role` | 角色：`entry`录入 `reviewer`复查 `safety_admin`安全管理员 `admin`系统管理员 |
| operation_log | `action` | 操作类型（login/create_hazard/review_hazard/export_hazard 等） |

### 6.4 常用查询

```sql
-- 看全部隐患
SELECT hazard_code, location, description, level, status, plan_deadline, rectify_person FROM hazard ORDER BY created_at DESC;

-- 只看未闭环的
SELECT hazard_code, location, status, plan_deadline FROM hazard WHERE status <> 'closed' ORDER BY plan_deadline;

-- 看用户
SELECT user_name, role, created_at FROM users ORDER BY created_at;

-- 看最近 20 条操作日志
SELECT user_name, action, target_code, detail, created_at FROM operation_log ORDER BY created_at DESC LIMIT 20;

-- 清空所有登录会话（所有人需重新登录）
DELETE FROM sessions;
```

### 6.4 应急：忘记 admin 密码怎么办

用 DBeaver 执行：
```sql
DELETE FROM users WHERE user_name = 'admin';
```
然后**重启服务**——启动时会自动重建 `admin / 123456`。
（注意：这样会把 admin 用户删掉再重建，隐患数据不受影响。）

---

## 七、Git 版本管理：改坏的后悔药

### 7.1 Git 需要什么账号密码？

**本地仓库不需要任何账号密码！** Git 只在把代码**推送到远程仓库**（如 Gitee、GitHub）时才需要登录。

本地使用只需要设置一个**显示用的名字和邮箱**（仅用于标记"这次提交是谁做的"）：

```bat
git config --global user.name "张三"
git config --global user.email "zhangsan@example.com"
```

> 这两条信息随便填真实的名字和邮箱即可，**不会联网验证**，也不会要密码。

### 7.2 本项目已连接的 GitHub 远程仓库

| 项 | 值 |
|----|----|
| 仓库地址 | https://github.com/nezu9ko/hazard-ledger |
| 默认分支 | `main` |
| 认证方式 | 浏览器登录（Git 凭据管理器已保存，**以后不用再登录**） |
| 代理 | 已配置**仅 github.com** 走代理 `127.0.0.1:7877`（见 7.3） |

**日常同步代码到 GitHub**（改完代码、提交后执行）：

```bat
git push
```

> 也可以直接双击项目里的 **`推送到GitHub.bat`**（首次使用会引导设置远端）。

**把 GitHub 上的改动拉回本地**（如果在网页上改过东西）：

```bat
git pull
```

### 7.3 ⚠️ GitHub 必须开代理

本机网络**直连 github.com 不通**，必须开启系统代理（`127.0.0.1:7877`）。

已给 Git 配好**仅对 github.com 生效**的代理（不影响 Gitee 等其他站点）：

```bat
:: 查看当前配置
git config --global --get-regexp proxy
:: 应显示：http.https://github.com.proxy http://127.0.0.1:7877
```

**如果推送时报 `Failed to connect to github.com`** → 检查系统代理是否开着；代理端口变了的话重新配置：

```bat
git config --global http.https://github.com.proxy "http://127.0.0.1:新端口"
```

### 7.4 日常使用的 4 条命令

在项目目录（`D:\hazard-ledger-lan`）打开终端：

```bat
git status                          # 看这次改了哪些文件
git add .                           # 把改动加入本次提交
git commit -m "改了台账筛选条件"     # 提交，说明改成什么
git log --oneline -10               # 看最近 10 次提交记录
```

### 7.5 改坏了怎么回到上一个版本

**情况 A：只是文件改乱了，还没提交**
```bat
git checkout .                      # 丢弃全部未提交的改动，回到上次提交的状态
```

**情况 B：已经提交了，才发现上一版更好**
```bat
git log --oneline                   # 找到想回到的那次提交号（如 a1b2c3d）
git checkout a1b2c3d -- .           # 把那次的文件内容恢复过来
git commit -m "回滚到上一版"
```

**情况 C：只想看某次改了什么**
```bat
git show a1b2c3d                    # 显示那次提交的详细改动
```

### 7.6 建议的使用节奏

```
动手改之前 → git commit -m "改动前的存档"
改完能跑通 → git commit -m "改了什么"
```

这样任何一次改坏都能回到"上一次能跑通"的状态。

---

## 八、故障排查：症状 → 原因 → 处理

### ★ 遇到问题，先跑一次「环境体检」

**双击项目根目录的 `检查环境.bat`**，会打印一份完整体检报告：

```
==============================================================
        隐患治理台账系统 — 环境体检报告
==============================================================
【一、系统服务】   Web 服务 / 接口健康 / 数据库服务 / 局域网地址
【二、数据库数据】 连接状态 / 隐患条数 / 用户数 / 日志条数
【三、自动启动与防火墙】 开机自启任务 / 每日备份任务 / 防火墙规则
【四、备份与照片】 最近备份时间 / 备份份数 / 照片数量与占用
【五、磁盘空间】   D 盘剩余空间
【六、代码与版本库】 分支 / 最近提交 / 是否有未提交改动
【七、近期运行错误】 仅报「本次运行期」的错误（历史错误自动忽略）
==============================================================
  体检完成： 正常 11 项 ｜ 注意 2 项 ｜ 异常 0 项
==============================================================
```

**报告里的三种标记**：
- `[正常]` 一切正常，无需处理
- `[注意]` 建议留意（不影响使用）
- `[异常]` **需要处理**，后面直接给出了处理办法

> 该脚本只读不写，不会改动任何数据或配置，可放心反复运行。

### 对症排查表

| 症状 | 可能原因 | 处理 |
|------|---------|------|
| **局域网同事打不开** | ①防火墙没放行 3000<br>②服务没启动<br>③不在同一网段 | ①检查防火墙规则 `HazardLedger 3000` 是否存在<br>②`netstat -ano \| findstr :3000`<br>③互相 ping 一下 |
| 本机能打开，别人打不开 | 同上（最常见是防火墙） | 以管理员执行：<br>`netsh advfirewall firewall add rule name="HazardLedger 3000" dir=in action=allow protocol=TCP localport=3000 profile=any` |
| **改了后端代码不生效** | 没重启服务 / 旧进程没被杀掉 | 双击 `restart-service.bat`（管理员） |
| **改了前端不生效** | 浏览器缓存 | `Ctrl` + `F5` 强制刷新 |
| 服务启动失败 | ①PostgreSQL 没启动<br>②`config.json` 密码错 | ①`netstat -ano \| findstr :5432` 检查<br>②核对 `config.json` 的 `db.password` |
| 端口 3000 被占用 | 其他程序占用了 | 改 `config.json` 的 `port`，再 `restart-service.bat` |
| 登录提示"账号或密码错误" | 密码不对 | 用 DBeaver 查 `users` 表；或按 6.4 重置 admin |
| 登录后一会儿就掉线 | 令牌过期（默认 7 天） | 重新登录即可；要改时长改 `server.js` 的 `SESSION_TTL_MS` |
| 上传照片失败 | 图片太大 | 前端会自动压缩；若仍失败检查 `server.js` 的 `MAX_IMAGE_BYTES` |
| 照片显示不出来 | 文件被误删 | 检查 `uploads\` 目录；用「系统维护 → 清理无引用图片」无关，该功能只删孤儿文件 |
| 导出的 Excel 打不开 | 下载中断 | 重新导出；确认磁盘有空间 |
| **打印件底部有网址/页码** | 浏览器自带的页眉页脚 | 打印预览 →「更多设置」→ 取消勾选「页眉和页脚」 |
| 看板图表是空的 | 没有对应数据 | 正常；有数据后会显示 |
| `Get-ScheduledTask` 查不到任务 | 非管理员查询看不到 SYSTEM 任务 | **这是假象**。看 `deploy-setup.log` 或直接看端口是否 LISTENING |
| 服务偶尔自己停了 | 见下方说明 | 重跑 `install-autostart.bat` 恢复 |

### 若服务意外停止

以管理员身份运行 **`install-autostart.bat`**，它会重建计划任务并立即启动服务。

---

## 九、备份与恢复

### 9.1 自动备份（已在运行）

- 计划任务 `HazardLedgerBackup` 每天 **02:00** 自动备份
- 输出到 `backup\hazard_YYYYMMDD_HHMMSS.sql`
- **保留最近 14 份**，更早的自动删除

### 9.2 手动立刻备份一次

双击 `backup-db.bat`，或管理员执行 `schtasks /Run /TN HazardLedgerBackup`。

### 9.3 恢复某个备份

```bat
"D:\PostgreSQL\pgsql\bin\psql.exe" -U postgres -h 127.0.0.1 -d hazard_ledger -f "D:\hazard-ledger-lan\backup\hazard_20260921_110259.sql"
```
（会提示输入数据库密码）

> ⚠️ 恢复是**覆盖式**的，会先删掉现有表再重建。恢复前建议先手动备份一次当前数据。

### 9.4 更彻底的备份：整个项目目录

因为数据库目录（`pgdata`）和照片目录（`uploads`）都在项目里，**把整个 `D:\hazard-ledger-lan` 复制一份就是完整备份**。

正确做法（保证数据一致）：
1. 停止两个服务：`net stop PostgreSQL-17` 和 `schtasks /End /TN HazardLedger`（管理员）
2. 复制整个 `D:\hazard-ledger-lan` 到移动硬盘或其他位置
3. 启动服务：`net start PostgreSQL-17`，然后 `schtasks /Run /TN HazardLedger`

### 9.5 建议的备份策略

| 频率 | 方式 | 保存位置 |
|------|------|---------|
| 每天 | 程序自动（已配置） | `backup\` 目录 |
| 每周 | 复制 `backup\` 最新一份 | 移动硬盘 / 网盘 |
| 每月 | 完整复制项目目录（停机操作） | 移动硬盘 |

---

## 十、安全注意事项

### 10.1 系统内置的三道账号安全防线

| 机制 | 行为 | 可调参数 |
|------|------|---------|
| **登录失败锁定** | 连续输错 **5 次** → 锁定该账号 **15 分钟**；锁定期间即使密码正确也拒绝。成功登录会清零计数 | `server.js` 的 `MAX_LOGIN_FAILS` / `LOGIN_LOCK_MINUTES` |
| **首次登录强制改密** | 新建用户、重置密码后，**改密前只能改密、不能使用任何其他功能**（服务端强制，绕不过去） | — |
| **口令慢哈希（scrypt）** | 数据库里的口令用 scrypt 存储，即使库文件泄露也难以离线破解 | `server.js` 的 `SCRYPT`（N/r/p） |

> 💡 **被锁定了怎么办？**
> - 等 15 分钟自动解锁；
> - 或让管理员在「用户管理」里**重置密码**（重置后须重新设置）；
> - 应急：重启服务（锁定计数在内存里，重启即清空）。
>
> 💡 **老数据会自动升级**：早期用 SHA-256 存的口令，用户下次登录成功时会**自动就地换成 scrypt**，无需人工干预。

### 10.2 日常注意事项

| 事项 | 说明 |
|------|------|
| **`config.json` 含数据库密码** | 不要发给外人，不要截图外传 |
| **上传目录可匿名读取** | `/uploads/*` 不需要登录（`<img>` 无法带鉴权头），文件名是 24 位随机数、不可枚举；**敏感文件请勿上传** |
| **`pgdata\` 是数据库本体** | 不要手动删改里面的文件，会让数据损坏 |
| **`uploads\` 是附件本体** | 同上；删了附件就没了（记录还在，但打不开） |
| **上线后立即改 admin 密码** | 初始密码 `123456` 所有人都知道 |
| **不要把 3000 端口映射到公网** | 本系统按内网设计，没有 HTTPS |
| 数据库只监听本机 | 这是刻意的安全设计，不要改成对外监听 |
| 文件名不要用中文 | 改代码文件时用英文名，避免编码问题 |
| 关闭"隐藏扩展名" | 资源管理器 → 查看 → 勾选「文件扩展名」，避免把 `.js` 存成 `.js.txt` |

---

## 十一、本机已装工具（维护时用）

| 用途 | 工具 | 位置 / 说明 |
|------|------|------------|
| **改代码** | **VS Code**（已安装） | `D:\VSCode\Code.exe`；桌面与开始菜单均有快捷方式；命令行 `code` 可用 |
| **看/改数据库** | **DBeaver**（安装包已下载，待你自行安装） | 安装包在 `C:\Users\admin\Downloads\dbeaver-ce-latest-x86_64-setup.exe` |
| 版本管理 | Git（已安装） | 本项目的仓库已初始化，见第七章 |
| 运行时 | Node.js | `D:\nodejs`（`node -v` 可验证） |
| 数据库 | PostgreSQL 17 | 程序 `D:\PostgreSQL\pgsql`，数据 `D:\hazard-ledger-lan\pgdata` |

### 用 VS Code 维护的推荐配置

1. **打开项目**：VS Code →「文件 → 打开文件夹」→ 选 `D:\hazard-ledger-lan`
2. **中文界面**：**已预装并已启用**（语言包 `ms-ceintl.vscode-language-pack-zh-hans`，已在 `argv.json` 设置 `"locale":"zh-cn"`），打开即为中文
3. **打开终端**：`Ctrl` + `` ` ``，可直接跑 `node server.js` 看报错
4. **建议扩展**：打开项目时 VS Code 会自动提示安装（`Prettier`、`SQLTools` 等），点「安装」即可

**项目已内置 `.vscode\` 配置**（随 Git 提交）：

| 文件 | 作用 |
|------|------|
| `settings.json` | 统一缩进/编码/换行；**已排除 `pgdata`、`node_modules`、`backup`**（关键！不排除会让编辑器卡顿、报文件监听错误） |
| `extensions.json` | 推荐扩展清单（打开项目时提示安装） |
| `launch.json` | 按 F5 调试 `server.js`（⚠️ 需先停掉后台服务，否则 3000 端口冲突） |

> 💡 `pgdata` 被 `settings.json` 隐藏了，所以左侧目录里看不到它 —— 这是**刻意设置**，避免误点误改数据库文件。它确实还在磁盘上。

### DBeaver 安装与连接（图形化查数据库）

**安装包**：`C:\Users\admin\Downloads\dbeaver-ce-latest-x86_64-setup.exe`

#### 安装时怎么选

| 安装向导的提问 | 怎么选 |
|---------------|--------|
| 安装位置 | 建议改到 D 盘（如 `D:\DBeaver`），C 盘空间紧张 |
| 装给谁（所有人/仅当前用户） | **仅为我安装**（不需要管理员权限） |
| 组件/快捷方式 | 勾**桌面快捷方式**即可 |
| **是否需要另装 Java** | **不需要**！DBeaver 自带 OpenJDK |

> ⚠️ **DBeaver 安装向导里没有"选择数据库"这一步**（官方文档已确认）。
> 安装包**不含数据库驱动**，驱动在软件内**首次连接时按需下载**。
> 你会看到"选择数据库类型"的地方是**打开软件后新建连接时**，不是安装时。

#### 新建连接步骤

1. 打开 DBeaver → 左上角「**新建数据库连接**」（插头图标）
2. 数据库类型选 **PostgreSQL**（**不要**选 "PostgreSQL (old)"）
3. 填写连接信息：

| 项 | 值 |
|----|----|
| 主机 / Host | `127.0.0.1` |
| 端口 / Port | `5432` |
| 数据库 / Database | `hazard_ledger` |
| 用户名 | `postgres` |
| 密码 | 见项目里 `config.json` 的 `db.password` |

4. **首次会提示下载驱动** → 点「Download」即可（已验证 Maven 中央仓库可达 ✅）
5. 点「Test Connection」→ 显示已连接后点「Finish」

#### 如果驱动下载失败

DBeaver 从 Maven 中央仓库下载 JDBC 驱动，下载失败时可：

1. **配代理**：DBeaver →「窗口 → 首选项 → 连接 → 代理」填入 `127.0.0.1:7877`（需先开系统代理）
2. **或换国内镜像**：首选项 → 连接 → 驱动 → Maven → 添加仓库 `https://maven.aliyun.com/repository/public`

#### 装好后能干什么

- 可视化查看 `hazard`（隐患）、`users`（用户）、`operation_log`（操作日志）等表
- 直接执行 SQL：忘记 admin 密码时可 `DELETE FROM users WHERE user_name='admin'` 再重启服务重建
- 导出查询结果

---

## 十二、附录：命令速查表

```bat
:: ========== 服务 ==========
netstat -ano | findstr :3000                              :: 看服务是否在跑
schtasks /Run /TN HazardLedger                            :: 启动服务
schtasks /End /TN HazardLedger                            :: 停止服务（可能残留进程）
schtasks /Query /TN HazardLedger /V /FO LIST              :: 查看任务详情

:: ========== 数据库 ==========
netstat -ano | findstr :5432                              :: 看数据库是否在跑
net start PostgreSQL-17                                   :: 启动数据库服务
net stop PostgreSQL-17                                    :: 停止数据库服务

:: ========== 备份 ==========
schtasks /Run /TN HazardLedgerBackup                      :: 立即备份一次
schtasks /Change /TN HazardLedgerBackup /ST 03:30         :: 改备份时间为 03:30

:: ========== 防火墙 ==========
netsh advfirewall firewall show rule name="HazardLedger 3000"   :: 查看规则

:: ========== Git ==========
git status                                                :: 看改了什么
git add . && git commit -m "说明"                          :: 提交
git log --oneline -10                                     :: 看历史
git checkout .                                            :: 丢弃未提交改动

:: ========== 手动跑服务（调试）==========
cd /d D:\hazard-ledger-lan && node server.js               :: 前台运行，能直接看到报错

:: ========== 连接数据库（命令行）==========
"D:\PostgreSQL\pgsql\bin\psql.exe" -U postgres -h 127.0.0.1 -d hazard_ledger
```

---

## 附：一页纸速记

```
改样子 → public\style.css / script.js → 刷新浏览器 Ctrl+F5
改逻辑 → server.js → 双击 restart-service.bat（管理员）
看报错 → 打开 server-out.log 拉到最后
改坏了 → git checkout .  （或把 .bak 文件改回来）
不清楚 → 先备份再动手；数据库用 DBeaver 连 127.0.0.1:5432/hazard_ledger
```
