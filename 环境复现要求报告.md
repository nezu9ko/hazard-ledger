# 隐患治理台账系统 — 环境复现要求报告

> 生成日期：2026-09-21
> 适用版本：本地部署版（`D:\hazard-ledger-lan`）

---

## 一、系统概述

一套**纯内网（局域网）**运行的隐患治理台账系统，用于「排查发现 → 登记上报 → 整改实施 → 复查验收 → 闭环销号」全流程管理。

| 项 | 说明 |
|----|------|
| 架构 | 单进程 Node 服务（同时提供 API 与前端静态资源）+ 本地 PostgreSQL |
| 前端 | 原生 HTML/CSS/JS 单页应用，**无框架、无构建、无 CDN 依赖**（可完全离线） |
| 后端 | Node.js 原生 `http` 模块，**无 Web 框架**；仅 1 个第三方依赖 `pg` |
| 数据 | 业务数据存 PostgreSQL；照片文件存本地 `uploads/` 目录 |
| 客户端 | 浏览器即可（Chrome / Edge / 手机浏览器），无需安装任何软件 |
| 并发规模 | 设计目标：局域网内**几十人**同时使用 |

---

## 二、环境要求

### 2.1 硬件（服务器，即运行本系统的那台电脑）

| 项 | 最低 | 建议 |
|----|------|------|
| CPU | 双核 | 4 核及以上 |
| 内存 | 2 GB | 4 GB 及以上 |
| 磁盘 | 2 GB 可用 | 10 GB 及以上（含数据库备份与照片） |
| 网络 | 与使用者在同一局域网 | 建议服务器用**固定内网 IP**（本机为 `192.168.2.22`） |

> 参考：本机为 i5-12400F / 16GB，服务进程常驻内存约 100 MB，PostgreSQL 约 200 MB。

### 2.2 操作系统

| 系统 | 支持情况 |
|------|----------|
| **Windows 10 / 11（x64）** | ✅ **本环境已完整验证**（Windows 11 Build 26200） |
| Windows Server 2016+ | ✅ 理论支持（脚本通用） |
| Linux / macOS | ⚠️ 服务端与前端代码跨平台可用，但 `*.bat` 运维脚本需改写为 shell |

### 2.3 软件依赖（必须）

| 软件 | 版本要求 | 本环境实测 | 说明 |
|------|----------|-----------|------|
| **Node.js** | ≥ 18（推荐 22 LTS） | **v22.22.2** | 仅需 Node 本身，无构建工具链 |
| **PostgreSQL** | ≥ 12 | **17.6** | 便携版或安装版均可 |
| npm | 随 Node 附带 | 10.9.7 | 仅用于安装 `pg` |

> **不需要**：Java / Python / .NET / Nginx / IIS / Docker。系统不占用 80/443 端口。

### 2.4 npm 依赖清单（仅 1 个直接依赖，14 个包）

```
直接依赖
└─ pg@8.23.0                    PostgreSQL 驱动

间接依赖（自动安装）
   pg-cloudflare@1.4.0    pg-connection-string@2.14.0   pg-int8@1.0.1
   pg-pool@3.14.0         pg-protocol@1.16.0            pg-types@2.2.0
   pgpass@1.0.5           postgres-array@2.0.0          postgres-bytea@1.0.1
   postgres-date@1.0.7    postgres-interval@1.2.0       split2@4.2.0
   xtend@4.0.2
```

> 说明：`node_modules` 总大小约 1 MB，可随项目一起拷贝到无外网的环境（离线部署）。

---

## 三、目录结构

```
D:\hazard-ledger-lan\
├── server.js                 # 【核心】服务端全部逻辑（API + 鉴权 + 静态托管 + XLSX 生成）
├── config.json               # 【配置】端口 + 数据库连接（含密码）★不可外泄
├── package.json              # 依赖声明（仅 pg）
├── package-lock.json         # 依赖版本锁定
├── .gitignore                # 排除 node_modules / config.json / pgdata / uploads / backup
├── README.md                 # 使用与运维说明
├── 环境复现要求报告.md         # 本文件
│
├── public\                   # 【前端】静态资源（服务端直接对外提供）
│   ├── index.html            #   入口页
│   ├── style.css             #   全部样式（含响应式适配）
│   ├── script.js             #   全部前端逻辑（SPA + SVG 图表）
│   └── logo.jpg              #   公司 Logo（738×762）
│
├── pgdata\                   # 【数据】PostgreSQL 数据目录（已迁入本项目）★勿手动改动
├── uploads\                  # 【数据】隐患照片 / 整改照片 ★勿手动改动
├── backup\                   # 【备份】数据库每日自动备份（保留最近 14 份）
│
├── start.bat                 # 手动前台启动
├── run-server.bat            # 后台启动（供计划任务调用，输出重定向到日志）
├── install-autostart.bat     # 【部署】放行防火墙 + 注册开机自启（需管理员）
├── restart-service.bat       # 【运维】重启服务（需管理员）
├── backup-db.bat             # 【运维】数据库备份脚本（由每日任务调用）
├── migrate-pgdata.bat        # 【运维】迁移 PostgreSQL 数据目录（含自动回滚）
│
└── *.log                     # 运行日志：server-out / deploy-setup / backup / restart 等
```

---

## 四、网络与端口

| 端口 | 用途 | 监听地址 | 是否对外开放 |
|------|------|----------|--------------|
| **3000** | 系统 Web 服务（前端 + API） | `0.0.0.0` | ✅ 需放行（局域网访问） |
| **5432** | PostgreSQL | `127.0.0.1`（仅本机） | ❌ 不对局域网开放（更安全） |

**访问地址**
- 服务器本机：`http://localhost:3000`
- 局域网其他电脑：`http://192.168.2.22:3000`（换成服务器的实际内网 IP）

**必需的两项系统配置**（`install-autostart.bat` 会自动完成）
1. **防火墙入站规则**：`HazardLedger 3000`（TCP 3000，域/专用/公用全部放行）
2. **开机自启计划任务**：`HazardLedger`（以 SYSTEM 运行，开机延迟 30 秒启动）

> ⚠️ Windows 防火墙默认会拦截 3000 端口，**不放行则局域网其他电脑无法访问**（本机自己却正常，容易误判）。

---

## 五、配置文件说明

`config.json`（**含数据库密码与公司名称，务必妥善保管；该文件不进版本库**）：

```json
{
  "port": 3000,                     // Web 服务端口
  "host": "0.0.0.0",                // 0.0.0.0 = 允许局域网访问；改成 127.0.0.1 则仅本机可访问
  "companyName": "公司名称",         // 显示在侧边栏与登录页；留空则不显示
  "db": {
    "host": "127.0.0.1",
    "port": 5432,
    "user": "postgres",
    "password": "在此填写你的数据库口令",  // ★数据库口令（本文件不进版本库）
    "database": "hazard_ledger"     // 库名（不存在时自动创建）
  }
}
```

> 💡 代码仓库中**不含公司名称**：公司名由 `config.json` 提供，服务端通过公开接口
> `/api/app-info` 下发给前端显示。因此换机器部署时**必须**填好 `companyName`。
> 可参考项目里的 `config.example.json` 模板。

**优先级**：环境变量 > `config.json` > 内置默认值。
可用环境变量覆盖：`PORT`、`HOST`、`PGHOST`、`PGPORT`、`PGUSER`、`PGPASSWORD`、`PGDATABASE`。

---

## 六、从零复现步骤（约 20 分钟）

### 步骤 1：安装 Node.js
1. 下载 Node.js **22 LTS**（x64）安装包，一路默认安装；
2. 验证：命令行执行 `node -v`，应显示 `v22.x`。

### 步骤 2：安装 PostgreSQL
**方式 A（推荐，便携版，不写注册表）**
1. 解压便携版到 `D:\PostgreSQL\pgsql`；
2. 初始化数据目录并启动（可用 `start-pg.bat`）；
3. 建议注册为 Windows 服务并设为自动启动（用 `install-service.bat`）。

**方式 B（官方安装版）**
1. 运行安装程序，设置 `postgres` 用户口令；
2. 记住你设置的口令（后面要填进 config.json）。

### 步骤 3：部署程序
1. 把 `D:\hazard-ledger-lan` 整个目录拷贝到目标机器（**含 `pgdata` 与 `uploads`**）；
2. 若无外网，`node_modules` 也一并拷贝（可跳过步骤 4）；
3. 进入目录执行：
   ```bat
   cd /d D:\hazard-ledger-lan
   npm install
   ```

### 步骤 4：修改配置
编辑 `config.json`，把 `db.password` 改成实际数据库口令；若端口冲突则改 `port`。

### 步骤 5：首次启动
```bat
node server.js
```
预期输出：
```
  矿山安全隐患排查治理台账系统 — 本地服务已启动
  本机访问:   http://localhost:3000
  局域网访问: http://192.168.2.22:3000
  数据库:     PostgreSQL postgres@127.0.0.1:5432/hazard_ledger
  默认账号:   admin / 123456
```
首次启动会自动：**建库 `hazard_ledger` → 建表（hazard / users / counters / sessions / operation_log）→ 播种管理员 `admin / 123456`**。

### 步骤 6：配置开机自启与防火墙
右键 **以管理员身份运行** `install-autostart.bat`，它会：
1. 添加防火墙入站规则 `HazardLedger 3000`；
2. 删除并重建计划任务 `HazardLedger`（开机自启、以 SYSTEM 运行、延迟 30 秒）；
3. 立即启动一次服务。

### 步骤 7：验证
用浏览器打开 `http://<服务器内网IP>:3000`，用 `admin / 123456` 登录。

---

## 七、验证清单（部署后逐项确认）

| # | 检查项 | 命令 / 方式 | 预期 |
|---|--------|-------------|------|
| 1 | 服务在监听 | `netstat -ano \| findstr :3000` | 出现 `0.0.0.0:3000 LISTENING` |
| 2 | 本机可访问 | 浏览器 `http://localhost:3000` | 出现登录页 |
| 3 | 局域网可访问 | 另一台电脑浏览器访问 | 出现登录页 |
| 4 | 健康检查 | `http://<IP>:3000/api/health` | `{"ok":true,...}` |
| 5 | 数据库连通 | 登录成功 | 进入统计看板 |
| 6 | 未登录拦截 | 直接访问 `http://<IP>:3000/api/stats` | 返回 **401** |
| 7 | 计划任务 | `deploy-setup.log` 末尾 | `TaskName: \HazardLedger` / `Status: Running` |
| 8 | 自动备份 | 查看 `backup\` 目录 | 存在 `hazard_YYYYMMDD_HHMMSS.sql` |
| 9 | 数据落库 | 登录后新建一条隐患 | 台账列表出现该条 |

> ⚠️ 注意：**非管理员权限的 `Get-ScheduledTask` 查询不到以 SYSTEM 运行的任务**，会误显示"未找到"。判断任务是否存在请看 `deploy-setup.log`，或直接看端口是否 LISTENING。

---

## 八、日常运维

| 操作 | 方式 |
|------|------|
| 查看服务状态 | `netstat -ano \| findstr :3000` 或看 `server-out.log` 末尾 |
| 重启服务 | 双击 `restart-service.bat`（选"以管理员身份运行"） |
| 停止服务 | 以管理员运行 `schtasks /End /TN HazardLedger` |
| 手动备份 | `schtasks /Run /TN HazardLedgerBackup` 或双击 `backup-db.bat` |
| 改端口 / 数据库配置 | 编辑 `config.json` → 运行 `restart-service.bat` |
| 查看访问日志 | `server-out.log`（记录每个 API 的耗时与状态码） |

**自动备份**：计划任务 `HazardLedgerBackup` 每天 **02:00** 执行 `backup-db.bat`，输出到 `backup\hazard_<时间戳>.sql`，**保留最近 14 份**（更早自动删除）。

**数据恢复**：
```bat
"D:\PostgreSQL\pgsql\bin\psql.exe" -U postgres -h 127.0.0.1 -d hazard_ledger -f "backup\hazard_20260921_110259.sql"
```

**整体搬迁**（换机器）：
1. 停止 `PostgreSQL-17` 与 `HazardLedger` 两个服务；
2. 拷贝 `D:\hazard-ledger-lan`（含 `pgdata`、`uploads`）与 `D:\PostgreSQL\pgsql`；
3. 在新机器上以管理员运行 `D:\PostgreSQL\install-service.bat` 重新注册数据库服务，再运行 `install-autostart.bat`。

---

## 九、安全与边界（务必知悉）

| 项 | 现状 | 建议 |
|----|------|------|
| 对外暴露 | **仅内网**，PostgreSQL 只监听本机 | 不要做端口映射到公网 |
| 传输加密 | HTTP 明文 | 内网可接受；若走公网须加 HTTPS 反向代理 |
| 口令存储 | SHA-256(salt+口令) | 内网够用；公网建议换 bcrypt/argon2 |
| 初始口令 | 全部新用户为 `123456`，首登强制修改 | 上线后立即改掉 `admin` 口令 |
| 登录防爆破 | **无失败次数限制** | 内网可接受；如需可加限流 |
| 照片访问 | `/uploads/*` 不校验登录（`<img>` 无法带鉴权头） | 文件名 24 位随机，不可枚举 |
| 配置文件 | `config.json` 明文存库口令 | 已加入 `.gitignore`，注意目录权限 |
| CSRF | 天然免疫（令牌放请求头，不依赖 Cookie） | — |

---

## 十、代码自查报告

对 `server.js`（1359 行）、`public/script.js`（1647 行）、`public/style.css`、各 `*.bat` 做了逐项通读与专项排查。

### 10.1 已确认良好（通过检查）

| 检查项 | 结论 |
|--------|------|
| **SQL 注入** | 全部使用参数化查询（`$1/$2`）。仅 3 处模板字符串拼接 SQL：`CREATE DATABASE "库名"`（来自本地配置）、`UPDATE hazard SET <列名>`（列名取自**白名单常量**）、`SELECT ... FROM operation_log <where>`（由固定片段+占位符拼成）。**无用户输入直接进入 SQL 拼接** |
| **XSS** | 前端所有动态内容均经 `esc()` 转义；`openModal`/`toast` 内部对 `title`/`desc` 二次转义；唯一的未转义点是枚举值用于拼 class 名（level/status/role 均服务端白名单校验），**不可注入** |
| **鉴权覆盖** | 路由分发处**统一总闸**：除 `/api/health`、`/api/auth` 外，所有 `/api/*` 必须先通过令牌校验（401）再通过角色校验（403），**无遗漏端点** |
| **越权** | 已实测：entry/reviewer 角色访问用户管理、操作日志、删除隐患等均返回 **403** |
| **编号并发** | 隐患编号使用 `INSERT ... ON CONFLICT DO UPDATE ... RETURNING` 原子取号，**无撞号风险** |
| **路径穿越** | 静态托管与上传托管均校验解析后的真实路径必须位于目标目录内，文件名正则白名单 |
| **上传安全** | 类型白名单（JPG/PNG/WEBP/GIF）+ 体积上限 8MB + 随机文件名 |
| **照片引用** | 服务端 `normalizePhotos` 只接受 `/uploads/` 前缀，**外链与 `/etc/passwd` 之类注入会被直接丢弃**（已实测） |
| **日期口径** | 列表、筛选、统计三处统一使用本地日期，**不存在旧版"逾期口径差一天"的问题** |
| **异常兜底** | HTTP handler 外层 try/catch 统一转 500；数据库初始化带重试；写日志失败不影响主流程 |
| **优雅退出** | 监听 SIGINT/SIGTERM，关闭 HTTP 服务与连接池 |

### 10.2 本次自查发现并已修复

| # | 问题 | 影响 | 处理 |
|---|------|------|------|
| 1 | `handleHazards` 中 17 处 `badRequest("...")` **漏传 `res` 参数** | 参数校验失败时抛异常，返回 **500 而非 400**，前端提示"服务器内部错误" | 已全部改为 `badRequest(res, "...")` |
| 2 | 台账筛选区使用**内联样式** 4 列网格 | 媒体查询无法覆盖内联样式，手机上筛选框被压到 26px 导致**重叠** | 已改为 CSS 类 `.filter-grid` + 媒体查询 |
| 3 | `requiredRoles` 上方残留未使用的 `PUBLIC_PATHS` 常量 | 死代码，易误导 | 已删除 |
| 4 | 静态资源未找到时**统一返回首页 HTML + 200** | 请求不存在的图片/脚本会拿到 HTML，难以排查 | 已改为：带扩展名 → **404**；无扩展名 → SPA 兜底 |
| 5 | 早期数据"登记当天即闭环"（0 天） | 闭环时长图被拉低失真 | 已把演示数据调整为 3/4/5 天 |

### 10.3 已知风险与改进建议（未改动，供决策）

| # | 风险 | 级别 | 建议 |
|---|------|------|------|
| 1 | 口令散列为 SHA-256+盐，**非慢哈希** | 中 | 若能接触数据库文件，可离线暴力破解。内网可接受；公网务必换 bcrypt/argon2 |
| 2 | 登录**无失败次数限制** | 中 | 可加"连续失败 N 次锁定 M 分钟" |
| 3 | 初始口令统一 `123456` | 中 | 已强制首登修改；仍建议上线后立即改 `admin` 口令 |
| 4 | `/uploads/*` 无需登录即可访问 | 低 | 文件名随机不可枚举；若照片敏感可改为签名 URL |
| 5 | 照片"选择即上传"，未提交表单会留孤儿文件 | 低 | 已提供「系统维护 → 清理无引用图片」一键清理 |
| 6 | `/api/stats`、`/api/logs` 等方法未限定（POST 也可调用） | 低 | 无副作用，不影响正确性；可加 405 校验 |
| 7 | 新增用户时若传入重复 `userId` 会触发数据库唯一约束异常（500） | 低 | 前端不传该字段，实际不会触发；可加友好提示 |
| 8 | 过期会话仅在登录时清理 | 低 | 长期不登录会缓慢累积；可加定时清理 |
| 9 | `readBody` 将请求体整体读入内存为字符串 | 低 | 上限 16MB，局域网可控；如需再优化可改流式写盘 |

### 10.4 代码可维护性

| 文件 | 行数 | 注释行 | 注释率 |
|------|------|--------|--------|
| `server.js` | 1359 | 244 | **17%** |
| `public/script.js` | 1647 | 148 | **8%** |
| `public/style.css` | 425 | 27 | 6% |

已补充的注释类型：
- **文件头**：运行环境、依赖、架构图、代码结构导航、关键设计约定
- **分节注释**：配置 / 工具 / 领域模型 / 会话鉴权 / 权限矩阵 / 操作日志 / 各业务处理器 / 图表 / 页面 / 路由
- **函数注释**：关键函数说明**为什么这么做**（如为何用令牌而非 Cookie、为何自研 XLSX、为何本地日期）以及入参出参约定
- **易错点标注**：`⚠️` 标记（如"全系统日期口径统一走这里，绝不要用 toISOString"、"`badRequest` 的签名是 (res, message)，切勿漏传 res"）

---

## 十一、常见问题排查

| 现象 | 排查方向 |
|------|----------|
| 局域网其他电脑打不开 | ①服务器 `netstat -ano \| findstr :3000` 是否 LISTENING；②防火墙规则 `HazardLedger 3000` 是否存在；③两台机器能否互相 ping |
| 服务启动失败 | 看 `server-out.log`。常见：PostgreSQL 服务未启动、`config.json` 口令错误 |
| 端口 3000 被占用 | 改 `config.json` 的 `port`，再运行 `restart-service.bat` |
| 改完代码不生效 | 后端改动必须 `restart-service.bat`；前端（public/）改动只需刷新页面 |
| 重启后仍是旧进程 | `schtasks /End` **不会**杀掉 node 子进程；用 `restart-service.bat`（它会按端口找 PID 再 `taskkill /F /T`） |
| 打印件底部有网址/页码 | 这是**浏览器自带页眉页脚**，在打印设置里取消勾选「页眉和页脚」即可（应用内首次点击打印会提示一次） |
| 忘了 admin 口令 | 用 `psql` 直接改 `users` 表的 `password_hash`/`salt`，或删除该记录后重启服务（会自动重建 `admin/123456`） |

---

## 十二、复现所需材料清单

| 材料 | 来源 | 说明 |
|------|------|------|
| Node.js 22 LTS 安装包 | nodejs.org | 约 30 MB |
| PostgreSQL 17 便携版/安装包 | 官网 | 便携版约 890 MB |
| 项目目录 `hazard-ledger-lan` | 本机拷贝 | 含 `pgdata` 与 `uploads` 才是完整数据 |
| `pg` 依赖 | `npm install` 或直接拷贝 `node_modules` | 约 1 MB |
| 公司 Logo | `public/logo.jpg` | 已随项目 |
| 管理员账号 | 自动创建 | 首次启动生成 `admin / 123456` |
